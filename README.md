# ofir-resources

Public, readable + downloadable resources — agent rulesets and article companions.

Deployed as a static site (see `index.html`). Add a new document by:
1. Dropping the `.md` file in `docs/`.
2. Adding an entry to `manifest.json`.
3. Pushing to `main` — the connected Vercel project redeploys automatically.
